<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.prototypefirebase">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.teams.ViewTeamDetailActivity"></activity>
        <activity android:name=".ui.teams.CreateTeamActivity" />
        <activity android:name=".TaskDetail" />
        <activity android:name=".TaskActivity" />
        <activity android:name=".ui.TaskActivity" />
        <activity android:name=".BoardActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main" />
        <activity
            android:name=".DatabaseActivity"
            android:label="database_activity" />
        <activity
            android:name=".SignInActivity"
            android:label="@string/app_name"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
